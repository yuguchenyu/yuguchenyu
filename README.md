# Jiahao You — Academic Portfolio

This is a dependency-free static website. Upload `index.html` and `styles.css` to the root of a GitHub repository and enable GitHub Pages from the repository settings.

Suggested repository name: `yuguchenyu.github.io` for the address `https://yuguchenyu.github.io`.
